import FilterForm from "./filterForm";

export default function InputSection({}: {}) {
  return (
    <div className="border px-12 py-4 rounded-lg m-6 sm:w-auto">
      <FilterForm />
    </div>
  );
}
